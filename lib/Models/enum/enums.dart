enum MsgType { success, error, normal }

enum AppointmentLeftItemValue {
  Date,
  Time,
  Worker, //Dodaj pracownika
  Location, //Dodaj placówkę
  Payment,
}

enum AppointmentRightItemValue {
  Services,
  Products,
  Notes,
  Agreements,
}

enum JobType {
  Hairdresser,
  Manager,
  Owner,
  Others,
}
